[package]
name = "di-facade-consumer-renamed"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# Deliberately the only normal dependency; macro helpers must stay transitive.
app_di = { package = "lily_consumer", path = "../../../../crates/framework/lily_consumer" }
