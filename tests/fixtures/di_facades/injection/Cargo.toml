[package]
name = "di-facade-injection"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# Deliberately the only normal dependency; macro helpers must stay transitive.
lily_injection = { package = "lily_injection", path = "../../../../crates/foundation/lily_injection" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
