[package]
name = "di-facade-injection-renamed"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# Deliberately the only normal dependency; macro helpers must stay transitive.
app_di = { package = "lily_injection", path = "../../../../crates/lily_injection" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
